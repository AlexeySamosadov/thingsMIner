/// <reference types="node" />
import { Address, ContractProvider, Sender, SenderArguments } from '@ton/core';
import type { HighloadWalletV2 } from './HighloadWalletV2';
export declare class BatchSender implements Sender {
    private readonly provider;
    private readonly wallet;
    private readonly secretKey;
    private readonly batchSize;
    constructor(provider: ContractProvider, wallet: HighloadWalletV2, secretKey: Buffer, batchSize: number);
    get address(): Address;
    private readonly messages;
    clear(): void;
    send(args: SenderArguments): Promise<void>;
    submit(): Promise<void>;
}
